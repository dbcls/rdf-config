- DbscSNV:
  - subject:
    - $chr: col("hg38_chr")
    - $pos: col("hg38_pos")
    - $alt: col("alt")
    - $ref: col("ref")
    - $pra: join($pos, $ref, $alt, "-")
    - $hco: join("http://identifiers.org/hco/", $chr, "/GRCh38", "")
    - $var: join($hco, $pra, "#")
    - $var
  - objects:
    - variation_label: join($chr, $pos, $ref, $alt, "-")
    - alt_allele: $alt
    - ref_allele: $ref
    - position: $pos
    - reference: $hco

- DbscSNVEnsembl:
  - subject:
    - "Ensembl"
    - prepend("$var-")
  - objects:
    - is_ensembl:
      - col("Ensembl?")
      - split(";")
      - skip(".")
    - ensembl_region:
      - col("Ensembl_region")
      - split(";")
      - skip(".")
    - ensembl_gene_id:
      - col("Ensembl_gene")
      - split(";")
      - skip(".")
    - ensembl_functional_consequence:
      - col("Ensembl_functional_consequence")
      - split(";")
      - skip(".")
    - ensembl_id_c_change_p_change:
      - col("Ensembl_id_c.change_p.change")
      - split(";")
      - skip(".")

- DbscSNVRefSeq:
  - subject:
    - "RefSeq"
    - prepend("$var-")
  - objects:
    - is_refseq:
      - col("RefSeq?")
      - split(";")
      - skip(".")
    - refseq_region:
      - col("RefSeq_region")
      - split(";")
      - skip(".")
    - refseq_gene:
      - col("RefSeq_gene")
      - split(";")
      - skip(".")
    - refseq_functional_consequence:
      - col("RefSeq_functional_consequence")
      - split(";")
      - skip(".")
    - refseq_id_c_change_p_change:
      - col("RefSeq_id_c.change_p.change")
      - split(";")
      - skip(".")

- RandomForest:
  - subject:
    - $var
    - append("-RandomForest")
  - objects:
    - random_forest_score:
      - col("rf_score")
      - split(";")
      - skip(".")

- AdaBoost:
  - subject:
    - $var
    - append("-AdaBoost")
  - objects:
    - ada_boost_score:
      - col("ada_score")
      - split(";")
      - skip(".")
